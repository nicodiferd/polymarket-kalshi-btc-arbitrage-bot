# Kalshi Configuration
# Get these from: https://kalshi.com/account/api (or demo.kalshi.co for testing)
KALSHI_API_KEY_ID=your-api-key-id-here
KALSHI_PRIVATE_KEY_PATH=/path/to/your/kalshi_private_key.pem
KALSHI_USE_DEMO=true  # Set to 'false' for production trading

# Polymarket Configuration
# Export your wallet's private key (DO NOT share this with anyone!)
# Signature types: 0=EOA (MetaMask), 1=Email/Magic, 2=Browser wallet
POLYMARKET_PRIVATE_KEY=0x_your_private_key_here
POLYMARKET_FUNDER_ADDRESS=0x_your_polymarket_wallet_address
POLYMARKET_SIGNATURE_TYPE=1

# Trading Configuration
MAX_POSITION_SIZE=100    # Maximum dollars per trade
MIN_PROFIT_MARGIN=0.02   # Minimum $0.02 profit to execute
PAPER_TRADING=true       # Set to 'false' to execute real trades

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# KALSHI SETUP:
# 1. Go to https://kalshi.com/account/api (or demo.kalshi.co for testing)
# 2. Create a new API key
# 3. Download the private key .pem file
# 4. Save the .pem file somewhere secure (e.g., ~/.kalshi/private_key.pem)
# 5. Copy the API Key ID and set KALSHI_API_KEY_ID
# 6. Set KALSHI_PRIVATE_KEY_PATH to the full path of your .pem file
#
# POLYMARKET SETUP:
# 1. If using Email/Magic wallet (signature_type=1):
#    - Go to Polymarket settings
#    - Export your wallet's private key
#    - Your funder address is your Polymarket wallet address
#
# 2. If using MetaMask/EOA wallet (signature_type=0):
#    - Export private key from MetaMask
#    - Funder address = your MetaMask address
#    - You'll need POL (Polygon) for gas fees
#    - You'll need to approve token allowances before first trade
#
# 3. If using browser wallet (signature_type=2):
#    - Export private key from your wallet
#    - Funder address = your Polymarket proxy address
#
# SECURITY:
# - NEVER commit your .env file to git
# - NEVER share your private keys
# - Start with PAPER_TRADING=true to test
# - Use KALSHI_USE_DEMO=true to test with fake money first
# =============================================================================
